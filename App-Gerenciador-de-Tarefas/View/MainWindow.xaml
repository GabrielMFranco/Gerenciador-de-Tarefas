<Window x:Class="App_Gerenciador_de_Tarefas.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:App_Gerenciador_de_Tarefas"
		mc:Ignorable="d"
		Title="Gerenciamento de Tarefas" Height="450" Width="800">
	<StackPanel Orientation="Vertical" Margin="10">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="auto"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Label Grid.Row="0" Grid.Column="0" Content="Título:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
			<TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewTitle}" VerticalAlignment="Center"/>
			<Label Grid.Row="1" Grid.Column="0" Content="Descrição:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
			<TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewDescription}" VerticalAlignment="Center"/>
			<Label Grid.Row="2" Grid.Column="0" Content="Data de conclusão:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
			<TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewCompleted}" VerticalAlignment="Center"/>
		</Grid>
		<Button Content="Adicionar Tarefa"
				Margin="0,20"
				Background="CornflowerBlue"
				FontWeight="Bold"
				FontSize="14"
				Command="{Binding AddToDoItemCommand}"
				HorizontalAlignment="Center"
				Padding="10,5"/>
		<Grid HorizontalAlignment="Stretch">
			<Grid.RowDefinitions>
					<RowDefinition Height="auto"/>
				</Grid.RowDefinitions>
			<ListBox Grid.Row="0" ItemsSource="{Binding Tasks}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Auto" >
				<ListBox.ItemContainerStyle>
					<Style TargetType="ListBoxItem">
						<Setter Property="Template">
							<Setter.Value>
								<ControlTemplate TargetType="ListBoxItem">
									<Grid>
										<ContentPresenter/>
									</Grid>
								</ControlTemplate>
							</Setter.Value>
						</Setter>
					</Style>
				</ListBox.ItemContainerStyle>
				<ListBox.ItemTemplate>
					<DataTemplate >
						<Expander>
							<Expander.Header>
								<ContentControl>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="Auto"/>
										</Grid.ColumnDefinitions>
										<TextBlock Grid.Column="0" Text="{Binding Title}" Margin="5"  HorizontalAlignment="Stretch"/>
										<Button Grid.Column="1" Content="❌" Background="IndianRed"  
													Command="{Binding DataContext.RemoveToDoItemCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}}" 
													CommandParameter="{Binding}"/>
									</Grid>
								</ContentControl>
							</Expander.Header>
							<Expander.Style>
								<Style TargetType="Expander">
									<Setter Property="Background" Value="#00aaff"/>
									<Style.Triggers>
										<Trigger Property="IsExpanded" Value="True">
											<Setter Property="Background" Value="LightGreen"/>
										</Trigger>
									</Style.Triggers>
								</Style>
							</Expander.Style>
							<StackPanel x:Name="itemContainer">
								<TextBlock Text="{Binding Description}" Grid.Row="1" Grid.Column="0" Padding="5"/>
								<TextBlock Text="{Binding Create}" Grid.Row="2" Grid.Column="0" Padding="5"/>
								<TextBlock Text="{Binding Completed}" Grid.Row="3" Grid.Column="0" Padding="5"/>
							</StackPanel>
						</Expander>
						
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Grid>
	</StackPanel>
</Window>